package com.jdms.grad.cities.application.appstart;

public interface AppStartService {

    /**
     * performs business logic which should be run once app is started
     */
    void performAppStartActions();
}
